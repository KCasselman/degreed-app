export interface MovieMetadata {
    Title: string; // keeep
    Year: number; // keep
    Rated: string; // keep
    Release: Date; // keep - KC - ?type
    Runtime: string; // keep
    Plot: string; // keep
    Poster: string; // keep
    imdbID: number;
}